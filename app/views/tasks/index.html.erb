<h1>My Tasks</h1>
<ul style="list-style: none">
  <% @tasks.each do |task| %>
    <li>
      <input type="checkbox" name="<%= task.title %>" params="hello">
      <%= link_to task.title, task_path(task) %>
    </li>
  <% end %>
</ul>
<hr>
<%= link_to "Add new task", new_task_path %>
